include mixins/text

doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    title Focus
    include head/viewport
    include head/style
  body
    div(class = 'container container_back-img')
      .content
        .dummy
        .diamond
          .diamond__box
            +text (el + ' ' + mod)
          input(class= block + 'focus' readonly)
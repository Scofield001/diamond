include mixins/text

doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    include head/favicon
    title Focus
    include head/viewport
    include head/style
  body
    .container
      .content
        .dummy
        .diamond
          .diamond__box
            +text (el + ' ' + mod)
          input(class= block + 'focus' readonly)